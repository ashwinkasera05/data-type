#include<stdio.h>
void main()
{
    int a,b;
    printf("enter any no:");
    scanf("%d%d",&a,&b);
    if(a<b)
    {
        printf("a is smallest");
    }
    else
    {
        printf("a is not smallest");
    }
}