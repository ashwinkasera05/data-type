#include<stdio.h>
void main()
{
    int num;
    printf("enter at any no:");
    scanf("%d",&num);
    if(num%5==0)
    {
        printf("%d is divisble");
    }
    else
    {
        printf(" %d is not divisble");
    }
}
